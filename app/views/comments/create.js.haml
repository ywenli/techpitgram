// valはHTMLタグ内に記述されているvalue属性を取得したり変更できるメソッド
// val("")とすることで、入力したコメントの内容（value）を空に
$('#comment-post-#{@post.id.to_s}').
html('#{j render "posts/comment_list", { post: @post }}');
$('#comment-form-post-#{@post.id.to_s} #comment_comment').val("");
